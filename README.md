# Raycast URL Bundle Opener

A personal Raycast extension to open a bunch of predefined URLs with fuzzy search capabilities.

## Overview

This extension stores bundles of your URLs. You can fuzzy search through them and open the entire bundle in one go.

## To build

```bash

npm install
npm run build

```

